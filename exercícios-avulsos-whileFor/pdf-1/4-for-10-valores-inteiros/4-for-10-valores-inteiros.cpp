#include <stdio.h>
#include <stdlib.h>

int main(){

  int value, i, smaller, larger, mean;

  value=0, i=0, smaller=0, larger=0, mean=0;

  printf("Digite o primeiro valor");
  for(i=0 ; i>=9 ; i++){
    scanf("%d", &value);

  }

}